<div>
  <span>
    <h1>{{gender}} people</h1>
    <ul>

        {{#if gender}}
          no
        {{else}}
          {{#each people}}
            <li>{{this.first_name}} {{this.last_name}}  {{this.email}}</li>
          {{/each}}
        {{/if}}

    </ul>
  </span>
</div>
