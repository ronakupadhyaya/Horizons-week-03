<div>
	Students are . . . 
</div>

</div>
	<ul>
	{{#unless genderQuery}}

		{{#each data}}
			<li> {{this.first_name}} {{this.last_name}} {{this.email}} </li>
		{{/each}}

	{{/unless}}

	{{#if genderQuery}}

		{{#if male}}
			
			{{#each data}}

				{{#if_eq this.gender 'Male'}}

					<li> {{this.first_name}} {{this.last_name}} {{this.email}} </li>

				{{/if_eq}}

			{{/each}}
			

		{{/if}}

		{{#unless male}}

			{{#each data}}

				{{#if_eq this.gender 'Female'}}

					<li> {{this.first_name}} {{this.last_name}} {{this.email}} </li>

				{{/if_eq}}

			{{/each}}
		{{/unless}}
		
	
	
	{{/if}}

	</ul>

</div>
