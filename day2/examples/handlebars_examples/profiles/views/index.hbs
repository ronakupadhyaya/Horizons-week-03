<div>

  <ul>
    {{#if isMale}}
      {{#each data}}
      <li>{{this.first_name}} {{this.last_name}} {{this.email}} {{this.gender}}</li>
      {{/each}}
      {{/if}}


    {{#if isFemale}}
      {{#each data}}
      <li>{{this.first_name}} {{this.last_name}} {{this.email}} {{this.gender}}</li>
      {{/each}}
      {{/if}}
  </ul>
</div>
