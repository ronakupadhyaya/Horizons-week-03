<!DOCTYPE html>
<html>
	<head>
		<meta charset = "utf-8">
		<title> Page Title </title>
	</head>
	<body>
		<div>
  			<ul>
				{{#each data}}
					{{#if data.gender === male}}
						<li> {{this.first_name}} {{this.last_name}} </li>
					{{/if}}
				{{/each}}
			</ul>
		</div>
	</body>
</html>
