{
  "name": "fb",
  "version": "1.0.0",
  "description": "1. `npm init` in a new folder to create a project\r 1. create a new `app.js` file\r 1. you will need to `npm install` dependencies as you come across them\r 1. for now install nodemon using npm and add a start script similar to the following: `\"start\": \"nodemon app.js\"`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
