<h1>View Project</h1>

<!-- Exercise 3: View a single project -->

<!-- YOUR CODE HERE -->
<p>
  <div style='background-color: green'>
    <h3>{{project.title}}</h3>
    <h4>{{project.description}}</h4>
    <h3>{{project.goal}}</h3>
    <h5>{{project.start}}</h5>
    <h5>{{project.end}}</h5>
  </br>
  <h3>Contribute to this project:</h3>
  <form action ="/project/{{project.id}}" method="post">
      <div class="form-group">
          <input type="text" name="name" class="form-control" id="name"
                 placeholder="Your Name" value="{{data.name}}">
      </div>
      <div class="form-group">
          <input type="text" name="comment" class="form-control" id="comment"
                 placeholder="Comment" value="{{data.comment}}">
      </div>
      <div class="form-group">
          <div class="input-group">
              <div class="input-group-addon">$</div>
              <input type="text" class="form-control" id="amount" name="amount"
                     placeholder="Amount" value="{{data.amount}}">
              <div class="input-group-addon">.00</div>
          </div>
      </div>
      <button type="submit">Contribute</button>
  </form>
  <br/>
  <a href="/">Back to all projects</a>
  <form action = "/project/{{project.id}}/edit" method "post">
    <input type='submit' value = 'Edit Project'></button>
  </form>
  </div>
  <h1>{{total}}</h1>
  <div>
    {{#each project.contributions}}
      <h3>{{name}}
      <h4>{{comment}}
      <br>
    {{/each}}
  </div>
</p>

<!-- Exercise 4: Contribute to a project -->

<!-- YOUR CODE HERE -->
