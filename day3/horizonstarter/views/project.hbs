<h1>View Project</h1>

<div class="">
  <h2>{{a.title}}</h2>
  <h4>Goal: {{a.goal}}</h4>
  <h4>Started: {{a.start}}</h4>
  <h4>By: {{a.end}}</h4>
  <p>{{a.description}}</p>
  <ul>
    {{#each a.contributions}}
      <li>Contributor: {{name}} <br>Amount: {{amount}}</li>
    {{/each}}
  </ul>
</div>

<p>Total: {{a.total}}</p>
<div class="progress">
  <div class="progress-bar" role="progressbar" aria-valuenow="70"
  aria-valuemin="0" aria-valuemax="{{a.goal}}" style="width:{{complete}}%">
    <span>{{complete}}% Complete</span>
  </div>
</div>

<h3>Make a contribution:</h3>
<form class="" action="/project/{{id}}" method="post">
  <div class="form-group">
    <label for="" class="form-control">Name: </label>
    <input type="text" class="form-control" name="name" value="">
  </div>
  <div class="form-group">
    <label for="" class="form-control">Amount: </label>
    <input type="text" class="form-control" name="amount" value="">
  </div>
  <button type="submit" name="button">Submit</button>
</form>

<!-- YOUR CODE HERE -->

<!-- Part 4: Contribute to a project -->

<!-- YOUR CODE HERE -->
